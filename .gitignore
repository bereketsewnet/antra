# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.claude
dist-ssr
*.local

# Secrets — never commit
antra.email.config.md
antra.email.config.png
.env
.env.local
mail-config.php
# config.php reads its values from .env and holds no secrets, so it IS committed.
# The real secrets live in .env (ignored above).
# Uploaded CVs / private user data (should live outside web root in prod anyway)
server/private/
private/
# Uploaded public job images (kept out of git; recreated on the server)
server/uploads/


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
